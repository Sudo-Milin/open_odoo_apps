<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ============================================================
         Media Capture — Record Rules for ir.attachment
         ============================================================ -->

    <!-- Internal users: full access to attachments they created -->
    <record id="media_attachment_rule_user" model="ir.rule">
        <field name="name">Media Capture: User owns attachment</field>
        <field name="model_id" ref="base.model_ir_attachment"/>
        <field name="domain_force">[('create_uid', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

    <!-- Portal users: can create attachments, read their own -->
    <record id="media_attachment_rule_portal_read" model="ir.rule">
        <field name="name">Media Capture: Portal read own attachments</field>
        <field name="model_id" ref="base.model_ir_attachment"/>
        <field name="domain_force">[('create_uid', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
    </record>
</odoo>
